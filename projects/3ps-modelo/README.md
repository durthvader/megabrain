# 3Ps — Modelo executivo

Sandbox do modelo editável usado como referência para novas regionais.

`src/build_presentation.py` regenera o arquivo em `deliverables/`. O modelo é
interno e não possui portal público.
