# Pedidos úteis ao Codex

## Nova demanda

> Crie um sandbox para `<nome>`. O objetivo é `<objetivo>`, o público é
> `<público>`, a entrega será `<formato>` e as fontes estão em `<caminho>`.
> Registre o resultado no catálogo do Megabrain.

## Atualizar um projeto

> Atualize o sandbox `<id>` com os arquivos em `<caminho>`, preserve a versão
> anterior em `archive/`, valide o resultado e atualize status/data no catálogo.

## Portal com acesso restrito

> Prepare `public/` do projeto `<id>` para implantação independente. Não exponha
> tokens nem caminhos locais e liste os passos necessários para liberar somente
> o grupo `<grupo>`.

## Excluir um projeto

> Audite o sandbox `<id>` e mostre todos os recursos locais e externos que seriam
> afetados. Só depois da minha confirmação, remova o projeto e sincronize o
> catálogo.

## Usar um caminho local

> Os arquivos desta demanda estão em `C:\Users\rogerio.fonseca\Downloads\<pasta>`.
> Leia o conteúdo sem fazer upload ao Supabase, a menos que a ferramenta realmente
> precise da base online.
