# Dados locais e segredos — nunca publicar
.env
.env.local
.mcp.json
.claude/
assets/js/config.js
data/catalogo-projetos.json
data/catalogo-projetos.local.json

# Cada sandbox inteiro é local. O repositório público guarda apenas o sistema,
# a documentação e os exemplos que ficam diretamente em projects/.
projects/*/

# Componentes compartilhados também pertencem aos projetos, não ao portal.
packages/*/

# Dependências e artefatos
node_modules/

# Sistema operacional
.DS_Store
Thumbs.db

# A configuração real do Supabase permanece somente no ambiente local.
# Use assets/js/config.example.js como modelo em novos clones.

# Python e artefatos de inspecao
__pycache__/
*.inspect.ndjson
